const initialState = {}

export function usersReducer(
    previousState,
    action
) {
    const state = previousState || initialState
    switch (action.type) {
        default: 
            return state
    }
}